<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_generate_reviews_wizard_form" model="ir.ui.view">
        <field name="name">hr.generate.reviews.wizard.form</field>
        <field name="model">hr.generate.reviews.wizard</field>
        <field name="arch" type="xml">
            <form string="Generate Performance Reviews">
                <group>
                    <field name="cycle_id" readonly="1"/>
                    <field name="department_ids" widget="many2many_tags"/>
                    <field name="employee_ids" widget="many2many_tags"
                           domain="[('active', '=', True), ('employee_type', '=', 'employee')]"/>
                    <field name="skip_existing"/>
                </group>
                <footer>
                    <button name="action_generate" string="Generate Reviews"
                            type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

</odoo>
