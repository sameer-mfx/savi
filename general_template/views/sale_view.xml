<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Inherited Customer For add Reporting Field -->
        <record id="view_report_company_sale_order_template" model="ir.ui.view">
            <field name="name">sale.order.template.form.inherit</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name="model">sale.order</field>
            <field name="arch" type="xml">
                <xpath expr="//page[@name='customer_signature']/group/field[@name='signed_by']" position="before">
                    <field name="sale_is_show_signature"/>
                </xpath>
                <xpath expr="//field[@name='validity_date']" position="after">
                    <field invisible="1" name="report_template_id1"/>
                    <field domain="[('report_name', 'ilike', '%sale_order%')]" name="report_template_id" options="{'no_create': True}" invisible="state != 'draft'" string="Sale Quotation Template"/>
                    <field domain="[('report_name', 'ilike', '%sale_order%')]" name="report_template_id" options="{'no_create': True}" invisible="state == 'draft'"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>